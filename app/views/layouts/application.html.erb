<!DOCTYPE html>
<html lang="en">
  <head>
    <% page_title = content_for?(:title) ? "#{content_for(:title)} - Flexitime" : "Flexitime" %>
    <title><%= page_title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <%= favicon_link_tag "FLEXITIME_LOGO_ICON.png" %>
    <%= favicon_link_tag "FLEXITIME_LOGO_ICON.png", rel: "apple-touch-icon" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen antialiased">
    <header class="border-b border-black/5 bg-white/70 backdrop-blur">
      <div class="mx-auto flex max-w-5xl items-center justify-between px-6 py-4">
        <div class="flex items-center gap-3">
          <%= link_to root_path, class: "flex items-center gap-3" do %>
            <div class="flex h-10 w-10 items-center justify-center overflow-hidden rounded-full">
              <%= image_tag "FLEXITIME_LOGO_ICON.png", class: "h-8 w-8 rounded-full object-cover", alt: "Flexitime" %>
            </div>
            <%= image_tag "FLEXITIME_LOGO_TEXT_MONO.png", class: "h-10 w-auto mt-2", alt: "Flexitime" %>
          <% end %>
        </div>
        <% nav_link = "inline-flex items-center text-sm text-slate-600 transition hover:text-slate-900 pb-1" %>
        <% nav_link_active = "border-b-2 border-slate-900 text-slate-900" %>
        <% dropdown_link = "block px-4 py-2 text-sm text-slate-700 hover:bg-slate-50" %>
        <% dropdown_link_active = "bg-slate-50 text-slate-900 font-medium" %>
        <% time_entries_active = controller_name == "time_entries" && !%w[new create].include?(action_name) %>
        <% new_entry_active = controller_name == "time_entries" && %w[new create].include?(action_name) %>
        <% account_active = ["home", "passwords", "work_schedules", "sessions"].include?(controller_name) || controller_path == "identity/emails" %>
        <% create_user_active = controller_name == "registrations" %>
        <nav class="flex items-center gap-4">
          <% if Current.user %>
            <div class="hidden items-center gap-4 md:flex">
              <%= link_to "Time entries", root_path, class: [nav_link, (time_entries_active ? nav_link_active : nil)].compact.join(" ") %>
              <%= link_to "New entry", new_time_entry_path, class: [nav_link, (new_entry_active ? nav_link_active : nil)].compact.join(" ") %>
              <%= link_to "Account", account_path, class: [nav_link, (account_active ? nav_link_active : nil)].compact.join(" ") %>
              <% if Current.user.admin? %>
                <%= link_to "Create user", sign_up_path, class: [nav_link, (create_user_active ? nav_link_active : nil)].compact.join(" ") %>
              <% end %>
            </div>
            <details class="relative md:hidden">
              <summary class="btn-secondary cursor-pointer list-none">Menu</summary>
              <div class="absolute right-0 mt-2 w-48 overflow-hidden rounded-2xl border border-black/10 bg-white shadow-lg">
                <%= link_to "Time entries", root_path, class: [dropdown_link, (time_entries_active ? dropdown_link_active : nil)].compact.join(" ") %>
                <%= link_to "New entry", new_time_entry_path, class: [dropdown_link, (new_entry_active ? dropdown_link_active : nil)].compact.join(" ") %>
                <%= link_to "Account", account_path, class: [dropdown_link, (account_active ? dropdown_link_active : nil)].compact.join(" ") %>
                <% if Current.user.admin? %>
                  <%= link_to "Create user", sign_up_path, class: [dropdown_link, (create_user_active ? dropdown_link_active : nil)].compact.join(" ") %>
                <% end %>
              </div>
            </details>
          <% else %>
            <%= link_to "Sign in", sign_in_path, class: nav_link %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="mx-auto max-w-5xl px-6 py-10">
      <% flash.each do |key, message| %>
        <% base = "mb-6 rounded-2xl border px-4 py-3 text-sm" %>
        <% if key.to_s == "notice" %>
          <div class="<%= base %> border-brand/20 bg-brand/10 text-brand-dark"><%= message %></div>
        <% else %>
          <div class="<%= base %> border-rose-200 bg-rose-50 text-rose-900"><%= message %></div>
        <% end %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
