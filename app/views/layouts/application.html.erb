<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Flexitime" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <%= favicon_link_tag "FLEXITIME_LOGO_ICON.png" %>
    <%= favicon_link_tag "FLEXITIME_LOGO_ICON.png", rel: "apple-touch-icon" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen antialiased">
    <header class="border-b border-black/5 bg-white/70 backdrop-blur">
      <div class="mx-auto flex max-w-5xl items-center justify-between px-6 py-4">
        <div class="flex items-center gap-3">
          <%= link_to root_path, class: "flex items-center gap-3" do %>
            <div class="flex h-10 w-10 items-center justify-center overflow-hidden rounded-full">
              <%= image_tag "FLEXITIME_LOGO_ICON.png", class: "h-8 w-8 rounded-full object-cover", alt: "Flexitime" %>
            </div>
            <%= image_tag "FLEXITIME_LOGO_TEXT_MONO.png", class: "h-10 w-auto mt-2", alt: "Flexitime" %>
          <% end %>
        </div>
        <% nav_link = "text-sm text-slate-600 transition hover:text-slate-900" %>
        <nav class="flex items-center gap-4">
          <% if Current.user %>
            <%= link_to "Time entries", root_path, class: nav_link %>
            <%= link_to "New entry", new_time_entry_path, class: nav_link %>
            <%= button_to "Log out", Current.session, method: :delete, class: "btn-secondary", form: { class: "inline-block" } %>
          <% else %>
            <%= link_to "Sign in", sign_in_path, class: nav_link %>
            <%= link_to "Sign up", sign_up_path, class: "btn" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="mx-auto max-w-5xl px-6 py-10">
      <% flash.each do |key, message| %>
        <% base = "mb-6 rounded-2xl border px-4 py-3 text-sm" %>
        <% if key.to_s == "notice" %>
          <div class="<%= base %> border-brand/20 bg-brand/10 text-brand-dark"><%= message %></div>
        <% else %>
          <div class="<%= base %> border-rose-200 bg-rose-50 text-rose-900"><%= message %></div>
        <% end %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
